To use your own image:
1. Upload your photo to this `public` folder using the file explorer.
2. Ensure it gets named for example `portrait.jpg`.
3. Update the `src` attribute of the `<img>` tag in `src/App.tsx` from the Unsplash URL to `"/portrait.jpg"`.
